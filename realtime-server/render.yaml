services:
  - type: web
    name: growthyari-realtime
    runtime: node
    plan: free
    # Types are now in dependencies, so standard install works
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3001
      - key: REALTIME_SECRET
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
    healthCheckPath: /health
